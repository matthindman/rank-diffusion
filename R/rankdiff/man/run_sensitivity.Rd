% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{run_sensitivity}
\alias{run_sensitivity}
\title{Run sensitivity analysis across parameter perturbations}
\usage{
run_sensitivity(params, bundle, cfg, param_list = NULL, deltas = NULL)
}
\arguments{
\item{params}{Estimated parameters list.}

\item{bundle}{A \code{rankdiff_bundle} produced by
\code{\link{build_data_bundle}}.}

\item{cfg}{A \code{rankdiff_config}.}

\item{param_list}{Optional list of \code{c(display_name, attr_name)} vectors
(defaults to \code{SENSITIVITY_PARAMS}).}

\item{deltas}{Optional numeric vector of perturbation fractions (defaults to
\code{cfg$sensitivity_deltas}).}
}
\value{
A named list (keyed by display name) of named lists (keyed by delta),
each containing:
\describe{
\item{value}{Perturbed parameter value.}
\item{mc_means}{Named list of MC mean diagnostics.}
\item{pass_fail}{Named logical list.}
\item{n_pass}{Integer.}
\item{n_total}{Integer.}
}
}
\description{
For each parameter and each delta perturbation, creates a perturbed copy of
the estimated parameters, runs the Monte Carlo simulation, and computes
pass/fail diagnostics.
}
