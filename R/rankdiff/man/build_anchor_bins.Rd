% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initializers.R
\name{build_anchor_bins}
\alias{build_anchor_bins}
\title{Build log-spaced anchor bins from empirical moments}
\usage{
build_anchor_bins(bundle, cfg)
}
\arguments{
\item{bundle}{A \code{rankdiff_bundle} list (see \code{build_data_bundle}).}

\item{cfg}{A \code{rankdiff_config}.}
}
\value{
A data frame with one row per retained anchor bin and columns:
\code{rank_lo}, \code{rank_hi}, \code{rank_mid}, \code{z_center},
\code{n_entities}, \code{total_var}, \code{vr4}, \code{vr13},
\code{acf1}, \code{local_slope}, \code{kurtosis}.
}
\description{
Partitions tracked balanced entities into log-spaced rank bands and computes
per-band summary statistics (total variance, variance ratios, ACF(1),
kurtosis, local slope) used as fitting targets for the PT decomposition.
}
